// Diner section landing — Phase 0 stub.
//
// In Phase 1 this becomes the auth-gated diner home (offer browse).

export default function DinerHome() {
  return (
    <main className="flex flex-1 items-center justify-center px-6">
      <div className="max-w-md text-center space-y-3">
        <p className="font-mono text-xs tracking-widest uppercase text-muted-foreground">
          Phase 0
        </p>
        <h1 className="font-serif text-3xl font-semibold">Diner — coming soon</h1>
        <p className="text-sm text-muted-foreground">
          Sign-in and the offer browse arrive in Phase 1.
        </p>
      </div>
    </main>
  );
}
